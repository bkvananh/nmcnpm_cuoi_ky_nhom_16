<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Món Ăn - Catfe Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/header', { page: 'products' }) %>

    <div class="container">
        <!-- Header Actions -->
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
            <div>
                <h2 style="color: var(--admin-text); font-size: 1.8rem; font-weight: 800; margin-bottom: 5px;">
                    <i class="fas fa-mug-hot"></i> Danh Sách Món Ăn
                </h2>
                <p style="color: var(--admin-text-light); font-size: 0.9rem;">
                    Quản lý menu và sản phẩm của cửa hàng
                </p>
            </div>
            <a href="/admin/products/add" class="btn-sm btn-primary" style="padding: 12px 24px; font-size: 0.95rem; text-decoration: none;">
                <i class="fas fa-plus"></i> Thêm Món Mới
            </a>
        </div>

        <!-- Products Table Card -->
        <div class="card">
            <% if (products && products.length > 0) { %>
                <table class="table-custom">
                    <thead>
                        <tr>
                            <th style="width: 60px;">ID</th>
                            <th style="width: 100px;">Ảnh</th>
                            <th>Tên Món</th>
                            <th style="width: 120px;">Danh Mục</th>
                            <th style="width: 120px;">Giá</th>
                            <th style="width: 100px;">Nổi Bật</th>
                            <th style="width: 120px;">Hành Động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% products.forEach(item => { %>
                            <tr>
                                <!-- ID -->
                                <td>
                                    <span style="font-weight: 700; color: var(--admin-text-light);">
                                        #<%= item.id %>
                                    </span>
                                </td>
                                
                                <!-- Image -->
                                <td>
                                    <div style="width: 60px; height: 60px; border-radius: 8px; overflow: hidden; 
                                                background: rgba(255,255,255,0.1); box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
                                        <img src="<%= item.image_url %>" 
                                             alt="<%= item.name %>"
                                             style="width: 100%; height: 100%; object-fit: cover;"
                                             onerror="this.src='https://images.unsplash.com/photo-1509042239860-f550ce710b93?w=100&h=100&fit=crop'">
                                    </div>
                                </td>
                                
                                <!-- Name -->
                                <td>
                                    <span style="font-weight: 600; color: var(--admin-text);">
                                        <%= item.name %>
                                    </span>
                                </td>
                                
                                <!-- Category -->
                                <td>
                                    <% 
                                    let categoryInfo = {
                                        1: { name: 'Cà Phê', icon: 'mug-hot', color: '#8d6e63' },
                                        2: { name: 'Trà', icon: 'leaf', color: '#66bb6a' },
                                        3: { name: 'Freeze', icon: 'snowflake', color: '#42a5f5' },
                                        4: { name: 'Bánh', icon: 'cookie-bite', color: '#ffa726' }
                                    };
                                    let cat = categoryInfo[item.category_id] || { name: 'Khác', icon: 'question', color: '#888' };
                                    %>
                                    <span style="display: inline-flex; align-items: center; gap: 6px; 
                                                 padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; font-weight: 600;
                                                 background: <%= cat.color %>20; color: <%= cat.color %>;">
                                        <i class="fas fa-<%= cat.icon %>"></i>
                                        <%= cat.name %>
                                    </span>
                                </td>
                                
                                <!-- Price -->
                                <td>
                                    <span style="font-weight: 700; color: rgba(147, 102, 57, 0.9); font-size: 1rem;">
                                        <%= item.price.toLocaleString('vi-VN') %> đ
                                    </span>
                                </td>
                                
                                <!-- Featured -->
                                <td style="text-align: center;">
                                    <% if(item.is_featured) { %>
                                        <span style="display: inline-flex; align-items: center; gap: 5px;
                                                     padding: 5px 10px; border-radius: 12px; font-size: 0.8rem; font-weight: 700;
                                                     background: rgba(42, 157, 143, 0.2); color: #2a9d8f;">
                                            <i class="fas fa-star"></i> Có
                                        </span>
                                    <% } else { %>
                                        <span style="color: var(--admin-text-light); font-size: 0.85rem;">—</span>
                                    <% } %>
                                </td>
                                
                                <!-- Actions -->
                                <td>
                                    <a href="/admin/products/delete/<%= item.id %>" 
                                       class="btn-delete btn-sm" 
                                       onclick="return confirm('⚠️ Bạn có chắc muốn xóa món <%= item.name %>?');"
                                       style="padding: 8px 14px;">
                                        <i class="fas fa-trash"></i> Xóa
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <!-- Empty State -->
                <div style="text-align: center; padding: 80px 40px;">
                    <div style="width: 100px; height: 100px; margin: 0 auto 20px; border-radius: 50%;
                                display: flex; align-items: center; justify-content: center;
                                background: rgba(40, 54, 24, 0.1);">
                        <i class="fas fa-coffee" style="font-size: 3rem; color: rgba(40, 54, 24, 0.3);"></i>
                    </div>
                    <h3 style="color: var(--admin-text); margin-bottom: 10px;">Chưa có món nào</h3>
                    <p style="color: var(--admin-text-light); margin-bottom: 25px;">
                        Hãy thêm món đầu tiên vào menu của bạn!
                    </p>
                    <a href="/admin/products/add" class="btn-sm btn-primary" style="padding: 12px 30px; text-decoration: none;">
                        <i class="fas fa-plus"></i> Thêm Món Mới
                    </a>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>