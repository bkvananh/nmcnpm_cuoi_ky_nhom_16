<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Tin Tức - Catfe Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/header', { page: 'posts' }) %>

    <div class="container">
        <!-- Header -->
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
            <div>
                <h2 style="color: var(--admin-text); font-size: 1.8rem; font-weight: 800; margin-bottom: 5px;">
                    <i class="fas fa-newspaper"></i> Danh Sách Bài Viết
                </h2>
                <p style="color: var(--admin-text-light); font-size: 0.9rem;">
                    Quản lý tin tức, sự kiện và khuyến mãi
                </p>
            </div>
            <a href="/admin/posts/add" class="btn-sm btn-primary" style="padding: 12px 24px; text-decoration: none;">
                <i class="fas fa-pen-nib"></i> Viết Bài Mới
            </a>
        </div>

        <!-- Posts Table Card -->
        <div class="card">
            <% if (posts && posts.length > 0) { %>
                <table class="table-custom">
                    <thead>
                        <tr>
                            <th style="width: 60px;">ID</th>
                            <th style="width: 100px;">Hình ảnh</th>
                            <th>Tiêu đề</th>
                            <th style="width: 120px;">Loại</th>
                            <th style="width: 140px;">Ngày đăng</th>
                            <th style="width: 100px;">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% posts.forEach(post => { %>
                            <tr>
                                <!-- ID -->
                                <td>
                                    <span style="font-weight: 700; color: var(--admin-text-light);">
                                        #<%= post.id %>
                                    </span>
                                </td>
                                
                                <!-- Thumbnail -->
                                <td>
                                    <div style="width: 80px; height: 50px; border-radius: 8px; overflow: hidden; 
                                                background: rgba(255,255,255,0.1); box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
                                        <img src="<%= post.thumbnail_url %>" 
                                             alt="<%= post.title %>"
                                             style="width: 100%; height: 100%; object-fit: cover;"
                                             onerror="this.src='https://images.unsplash.com/photo-1497935586351-b67a49e012bf?w=160&h=100&fit=crop'">
                                    </div>
                                </td>
                                
                                <!-- Title -->
                                <td>
                                    <span style="font-weight: 600; color: var(--admin-text); line-height: 1.4;">
                                        <%= post.title %>
                                    </span>
                                </td>
                                
                                <!-- Type Badge -->
                                <td>
                                    <% if(post.type == 'event') { %>
                                        <span style="display: inline-flex; align-items: center; gap: 6px; 
                                                     padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; font-weight: 600;
                                                     background: rgba(100, 181, 246, 0.2); color: #42a5f5;">
                                            <i class="fas fa-calendar-alt"></i> Sự kiện
                                        </span>
                                    <% } else { %>
                                        <span style="display: inline-flex; align-items: center; gap: 6px; 
                                                     padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; font-weight: 600;
                                                     background: rgba(255, 167, 38, 0.2); color: #ffa726;">
                                            <i class="fas fa-gift"></i> Khuyến mãi
                                        </span>
                                    <% } %>
                                </td>
                                
                                <!-- Publish Date -->
                                <td>
                                    <span style="font-size: 0.9rem; color: var(--admin-text-light);">
                                        <i class="far fa-calendar"></i>
                                        <%= new Date(post.publish_date).toLocaleDateString('vi-VN') %>
                                    </span>
                                </td>
                                
                                <!-- Actions -->
                                <td>
                                    <a href="/admin/posts/delete/<%= post.id %>" 
                                       class="btn-delete btn-sm" 
                                       onclick="return confirm('⚠️ Xóa bài viết này?');"
                                       style="padding: 8px 14px;">
                                        <i class="fas fa-trash"></i> Xóa
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <!-- Empty State -->
                <div style="text-align: center; padding: 80px 40px;">
                    <div style="width: 100px; height: 100px; margin: 0 auto 20px; border-radius: 50%;
                                display: flex; align-items: center; justify-content: center;
                                background: rgba(40, 54, 24, 0.1);">
                        <i class="fas fa-newspaper" style="font-size: 3rem; color: rgba(40, 54, 24, 0.3);"></i>
                    </div>
                    <h3 style="color: var(--admin-text); margin-bottom: 10px;">Chưa có bài viết nào</h3>
                    <p style="color: var(--admin-text-light); margin-bottom: 25px;">
                        Hãy viết bài đầu tiên để chia sẻ tin tức với khách hàng!
                    </p>
                    <a href="/admin/posts/add" class="btn-sm btn-primary" style="padding: 12px 30px; text-decoration: none;">
                        <i class="fas fa-pen-nib"></i> Viết Bài Mới
                    </a>
                </div>
            <% } %>
        </div>
    </div>

    <script src="/js/admin.js"></script>
</body>
</html>