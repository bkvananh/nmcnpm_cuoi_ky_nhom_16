<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hộp Thư Liên Hệ - Catfe Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/header', { page: 'contacts' }) %>

    <div class="container">
        <!-- Header -->
        <div style="margin-bottom: 30px;">
            <h2 style="color: var(--admin-text); font-size: 1.8rem; font-weight: 800; margin-bottom: 5px;">
                <i class="fas fa-envelope"></i> Khách Hàng Liên Hệ
            </h2>
            <p style="color: var(--admin-text-light); font-size: 0.9rem;">
                Quản lý tin nhắn và yêu cầu hỗ trợ từ khách hàng
            </p>
        </div>

        <!-- Contacts Table Card -->
        <div class="card">
            <% if (contacts && contacts.length > 0) { %>
                <table class="table-custom">
                    <thead>
                        <tr>
                            <th style="width: 13%;">Ngày gửi</th>
                            <th style="width: 17%;">Họ Tên</th>
                            <th style="width: 20%;">Email</th>
                            <th style="width: 38%;">Nội dung</th>
                            <th style="width: 12%;">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% contacts.forEach(c => { %>
                            <tr>
                                <!-- Date -->
                                <td>
                                    <span style="color: var(--admin-text-light); font-size: 0.85rem;">
                                        <i class="far fa-calendar-alt"></i>
                                        <%= new Date(c.created_at).toLocaleString('vi-VN', { 
                                            day: '2-digit', 
                                            month: '2-digit', 
                                            year: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </span>
                                </td>
                                
                                <!-- Name -->
                                <td>
                                    <span style="font-weight: 700; color: var(--admin-text); display: flex; align-items: center; gap: 6px;">
                                        <i class="fas fa-user" style="color: rgba(244, 162, 97, 0.8);"></i>
                                        <%= c.name %>
                                    </span>
                                </td>
                                
                                <!-- Email -->
                                <td>
                                    <a href="mailto:<%= c.email %>" 
                                       style="color: rgba(100, 181, 246, 0.9); text-decoration: none; font-size: 0.9rem; display: flex; align-items: center; gap: 6px;">
                                        <i class="fas fa-envelope"></i>
                                        <%= c.email %>
                                    </a>
                                </td>
                                
                                <!-- Message Content -->
                                <td>
                                    <div style="max-height: 60px; overflow-y: auto; line-height: 1.5; color: var(--admin-text-light); font-size: 0.9rem;">
                                        <%= c.message %>
                                    </div>
                                </td>
                                
                                <!-- Actions -->
                                <td>
                                    <a href="/admin/contacts/delete/<%= c.id %>" 
                                       class="btn-delete btn-sm" 
                                       onclick="return confirm('⚠️ Xóa tin nhắn này?');"
                                       style="padding: 8px 14px;">
                                        <i class="fas fa-trash"></i> Xóa
                                    </a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <!-- Empty State -->
                <div style="text-align: center; padding: 80px 40px;">
                    <div style="width: 100px; height: 100px; margin: 0 auto 20px; border-radius: 50%;
                                display: flex; align-items: center; justify-content: center;
                                background: rgba(100, 181, 246, 0.15);">
                        <i class="fas fa-inbox" style="font-size: 3rem; color: rgba(100, 181, 246, 0.5);"></i>
                    </div>
                    <h3 style="color: var(--admin-text); margin-bottom: 10px;">Hộp thư trống</h3>
                    <p style="color: var(--admin-text-light);">
                        Hiện tại không có tin nhắn liên hệ nào từ khách hàng.
                    </p>
                </div>
            <% } %>
        </div>
    </div>
    
    <script src="/js/admin.js"></script>
</body>
</html>