<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Duyệt Bình Luận - Catfe Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/header', { page: 'comments' }) %>

    <div class="container">
        <!-- Header -->
        <div style="margin-bottom: 30px;">
            <h2 style="color: var(--admin-text); font-size: 1.8rem; font-weight: 800; margin-bottom: 5px;">
                <i class="fas fa-comments"></i> Kiểm Duyệt Bình Luận
            </h2>
            <p style="color: var(--admin-text-light); font-size: 0.9rem;">
                Quản lý và phê duyệt bình luận từ khách hàng
            </p>
        </div>

        <!-- Comments Table Card -->
        <div class="card">
            <% if (comments && comments.length > 0) { %>
                <table class="table-custom">
                    <thead>
                        <tr>
                            <th style="width: 15%;">Bài viết</th>
                            <th style="width: 15%;">Người gửi</th>
                            <th style="width: 35%;">Nội dung</th>
                            <th style="width: 12%;">Trạng thái</th>
                            <th style="width: 23%;">Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% comments.forEach(c => { %>
                            <tr>
                                <!-- Post ID -->
                                <td>
                                    <small style="color: var(--admin-text-light); font-weight: 600;">
                                        <i class="fas fa-newspaper"></i> 
                                        Bài ID: <%= c.post_id %>
                                    </small>
                                </td>
                                
                                <!-- Author -->
                                <td>
                                    <span style="font-weight: 700; color: var(--admin-text); display: flex; align-items: center; gap: 6px;">
                                        <i class="fas fa-user-circle" style="color: rgba(100, 181, 246, 0.8);"></i>
                                        <%= c.author_name %>
                                    </span>
                                </td>
                                
                                <!-- Content -->
                                <td>
                                    <span style="color: var(--admin-text-light); line-height: 1.5;">
                                        <%= c.content %>
                                    </span>
                                </td>
                                
                                <!-- Status Badge -->
                                <td>
                                    <% if(c.status == 'pending') { %> 
                                        <span style="display: inline-flex; align-items: center; gap: 6px; 
                                                     padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; font-weight: 700;
                                                     background: rgba(230, 126, 34, 0.2); color: #e67e22;">
                                            <i class="fas fa-clock"></i> Chờ duyệt
                                        </span> 
                                    <% } else { %> 
                                        <span style="display: inline-flex; align-items: center; gap: 6px; 
                                                     padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; font-weight: 700;
                                                     background: rgba(42, 157, 143, 0.2); color: #2a9d8f;">
                                            <i class="fas fa-check-circle"></i> Đã hiện
                                        </span> 
                                    <% } %>
                                </td>
                                
                                <!-- Actions -->
                                <td>
                                    <div style="display: flex; gap: 8px; flex-wrap: wrap;">
                                        <% if(c.status == 'pending') { %>
                                            <a href="/admin/comments/approve/<%= c.id %>" 
                                               class="btn-sm btn-success" 
                                               style="padding: 8px 14px;">
                                                <i class="fas fa-check"></i> Duyệt
                                            </a>
                                        <% } %>
                                        <a href="/admin/comments/delete/<%= c.id %>" 
                                           class="btn-delete btn-sm" 
                                           onclick="return confirm('⚠️ Xóa vĩnh viễn bình luận này?');"
                                           style="padding: 8px 14px;">
                                            <i class="fas fa-trash"></i> Xóa
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <!-- Empty State -->
                <div style="text-align: center; padding: 80px 40px;">
                    <div style="width: 100px; height: 100px; margin: 0 auto 20px; border-radius: 50%;
                                display: flex; align-items: center; justify-content: center;
                                background: rgba(42, 157, 143, 0.15);">
                        <i class="fas fa-comment-dots" style="font-size: 3rem; color: rgba(42, 157, 143, 0.5);"></i>
                    </div>
                    <h3 style="color: var(--admin-text); margin-bottom: 10px;">Sạch sẽ!</h3>
                    <p style="color: var(--admin-text-light);">
                        Không có bình luận nào cần kiểm duyệt. Tất cả đều đã được xử lý.
                    </p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="/js/admin.js"></script>
</body>
</html>