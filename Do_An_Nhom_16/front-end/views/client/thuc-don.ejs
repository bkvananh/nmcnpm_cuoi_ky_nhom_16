<!DOCTYPE html>
<html lang="vi">
<head>
    <%- include('../partials/head', { title: 'Thực Đơn - Catfe Coffee' }) %>
    
    <style>
        /* CSS Riêng cho trang Thực Đơn */
        .page-header {
            background-color: #f3eee6;
            padding: 40px 0;
            margin-bottom: 40px;
            text-align: center;
        }

        .page-title {
            font-size: 2.2rem;
            text-transform: uppercase;
            color: var(--primary);
            margin-bottom: 0;
        }

        /* Menu Tabs Styles */
        .menu-tabs {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 40px;
        }

        .tab-link {
            padding: 10px 25px;
            border: 1px solid var(--primary);
            border-radius: 30px; /* Bo tròn kiểu viên thuốc */
            color: var(--primary);
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .tab-link:hover {
            background-color: rgba(40, 54, 24, 0.1); /* Màu xanh nhạt khi hover */
        }

        /* Trạng thái Active: Nền xanh rêu, chữ trắng */
        .tab-link.active {
            background-color: var(--primary);
            color: var(--white);
        }
    </style>
</head>
<body>

    <%- include('../partials/header') %>

    <div class="page-header">
        <div class="container">
            <h1 class="page-title">Thực Đơn Catfe</h1>
<p class="mt-1">Khám phá hương vị đậm đà bản sắc Việt</p>
        </div>
    </div>

    <div class="container mb-2">
        <div class="menu-tabs">
            <a href="/thuc-don" 
               class="tab-link <%= (!currentCat) ? 'active' : '' %>">
               Tất cả
            </a>
            
            <a href="/thuc-don?cat=1" 
               class="tab-link <%= (currentCat == 1) ? 'active' : '' %>">
               Cà Phê
            </a>
            
            <a href="/thuc-don?cat=3" 
               class="tab-link <%= (currentCat == 3) ? 'active' : '' %>">
               Freeze
            </a>

            <a href="/thuc-don?cat=2" 
               class="tab-link <%= (currentCat == 2) ? 'active' : '' %>">
               Trà
            </a>

            <a href="/thuc-don?cat=4" 
               class="tab-link <%= (currentCat == 4) ? 'active' : '' %>">
               Bánh Ngọt
            </a>
        </div>

        <div class="product-grid section-padding" style="padding-top: 0;">
            <% if (products && products.length > 0) { %>
                <% products.forEach(function(item) { %>
                    <div class="product-card">
                        <div class="card-img-wrapper">
                            <img src="<%= item.image_url %>" 
                                 alt="<%= item.name %>" 
                                 onerror="this.src='/img/placeholder/product-default.jpg'">
                        </div>
                        <div class="card-body">
                            <h3 class="product-title"><%= item.name %></h3>
                            <p class="product-price"><%= item.price.toLocaleString('vi-VN') %> đ</p>
                            
                            <button class="btn btn-primary" onclick="alert('Hệ thống đang chuyển món <%= item.name %> sang giỏ hàng ShopeeFood...')">
                                Đặt Ngay
                            </button>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="text-center" style="grid-column: 1 / -1; padding: 50px;">
                    <i class="fas fa-coffee fa-3x" style="color: #ccc; margin-bottom: 20px;"></i>
                    <h3>Chưa có món trong danh mục này</h3>
                    <p>Vui lòng quay lại sau nhé!</p>
                </div>
            <% } %>
        </div>
    </div>

    <%- include('../partials/footer') %>

</body>
</html>